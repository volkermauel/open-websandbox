# Test dependencies for the runtime server unit tests.
# Runtime server deps come from agent-sandbox-platform/runtime/requirements-app.txt
# (fastapi, uvicorn[standard], python-multipart). This file adds only test tooling.
pytest>=8
pytest-asyncio>=0.23
httpx>=0.27
websockets>=12
