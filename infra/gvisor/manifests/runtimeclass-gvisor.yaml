# RuntimeClass for gVisor (cluster-scoped). Pods opt in with `runtimeClassName: gvisor`.
#
# Currently deployed on this cluster: Variant A (simple) — lands on any node that
# has the runsc handler (today: the gVisor worker nodes).
#
# When you dedicate + taint sandbox worker nodes (AgentSandbox.md §6.3), switch to
# Variant B by uncommenting the `scheduling:` block, after labelling/ainting nodes:
#   kubectl label node <node> sandbox.open-websandbox.dev/type=sandbox
#   kubectl taint  node <node> sandbox.open-websandbox.dev/type=sandbox:NoSchedule
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
#scheduling:
#  nodeSelector:
#    sandbox.open-websandbox.dev/type: sandbox
#  tolerations:
#    - key: sandbox.open-websandbox.dev/type
#      operator: Equal
#      value: sandbox
#      effect: NoSchedule
