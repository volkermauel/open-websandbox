//! Broker crate skeleton — real HTTP control plane lands in PR-C.
#![forbid(unsafe_code)]

/// Crate version marker; replaced by real metadata in PR-C.
pub fn version() -> &'static str {
    "wip"
}
